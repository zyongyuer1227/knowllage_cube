# 开发续接记录（2026-03-28 20:45）

## 1. 本文件目的

本文件记录 `2026-03-28` 晚间这一轮开发完成的功能、已修改的关键链路、当前行为边界、验证结果和后续续接注意事项。  
下一次继续开发时，优先参考本文件、根目录 `README.md` 和实际源码，不要只凭对话记忆继续改。

## 2. 本轮完成的核心开发

### 2.1 文档持久化增强

已将文档相关的本地持久化链路补齐，不再只依赖数据库中的 `markdown_content`：

- 上传原件保存到：
  - `storage/documents/<documentId>/source/`
- 转换后 Markdown 落盘：
  - `storage/documents/<documentId>/content.md`
- 转换后纯文本落盘：
  - `storage/documents/<documentId>/content.txt`
- 渲染后 HTML 快照落盘：
  - `storage/documents/<documentId>/preview.html`
- 文档资产元数据落盘：
  - `storage/documents/<documentId>/metadata.json`

后端在以下场景会自动同步这些资产：

- 上传并转换成功后
- 编辑正文后
- 回滚版本后
- 更新文档标题或归档路径后

### 2.2 转换任务恢复

后端已支持在服务启动时自动扫描并恢复 `pending` / `processing` 状态的转换任务，避免服务中断后任务悬空。

实现位置：

- `apps/backend/src/modules/document/document.service.ts`

### 2.3 欢迎页改造成系统持久化文档

“欢迎.md” 已不再是前端 `localStorage` 草稿，也不在游客文档树中出现。

当前设计为：

- 游客不可见、不可点击、不可选
- 游客未选中文档时，右侧使用它作为兜底欢迎页展示
- 管理员未选中真实文档时，右侧默认编辑的是这份欢迎文档
- 管理员保存后，写入服务器本地文件

实际落盘文件：

- `storage/system/welcome.md`
- `storage/system/welcome.json`
- `storage/system/welcome.preview.html`

对应接口：

- `GET /api/v1/system/welcome-document`
- `GET /api/v1/system/admin/welcome-document`
- `PUT /api/v1/system/admin/welcome-document`

### 2.4 管理员参数设置弹窗误关闭修复

已修复管理员“参数设置”弹窗在文本框中点击、拖拽选中内容时误关闭的问题。

现在行为改为：

- 只有在遮罩空白处按下并抬起，才会关闭弹窗
- 在弹窗内容区内点击、输入、选中文本，不会触发关闭

### 2.5 文档拖拽移动修复

已修复管理员界面中“拖拽文档到其他文件夹时只有动画、实际不移动”的问题。

处理方式：

- 对 `dataTransfer` 的读取增加标准类型兜底
- drop 时阻断事件冒泡
- 保证拖拽数据在不同浏览器 / 不同 DOM 命中情况下仍能读取到文档标识

### 2.6 “一键转换MD格式”改为 Python + 大模型

原先前端按钮只是本地调用 `normalizeToMarkdown()` 进行简单规整。  
现在已改为：

1. 管理员把原始文字粘贴进编辑区
2. 点击“一键转换md格式”
3. 前端请求后端接口：
   - `POST /api/v1/admin/documents/format-text-import`
4. 后端调用 Python 脚本：
   - `scripts/text2md_formatter.py`
5. Python 脚本通过大模型整理文本为 Markdown 风格 TXT
6. 后端再将生成结果接入现有文档落库 / 版本 / 预览链路

### 2.7 一键转换支持“就地格式化当前文档”

用户实际使用方式通常是：

1. 先点击“新增文档”
2. 输入标题
3. 粘贴原始文本
4. 点击“一键转换md格式”

此前新逻辑会再次尝试创建同名文档，触发标题唯一冲突。  
现在已改成：

- 如果当前选中的是已存在文档：
  - 直接将大模型格式化结果写回当前文档
- 如果当前不是已持久化文档：
  - 才走“导入为新文档”的流程

这解决了“先新增占坑文档再格式化”时的同名冲突问题。

### 2.8 文档标题冲突处理

截至 `2026-03-28 20:45` 的设计意图是把标题当作全局唯一业务键处理。  
但该行为已在 `2026-03-30` 调整为“保留唯一性目标，但在创建/导入/改名时自动避让重名”。

当前最新行为见本文第 `10` 节。

## 3. 新增脚本与接口

### 3.1 新增 Python 脚本

- `scripts/text2md_formatter.py`

作用：

- 读取原始文本输入
- 参考 `scripts/sample.md`
- 调用大模型输出 Markdown 风格 TXT

依赖前提：

- 环境变量 `DEEPSEEK_API_KEY`
- Python 环境可用 `openai`

### 3.2 新增后端 DTO / 接口

新增 DTO：

- `apps/backend/src/modules/document/dto/format-text-import.dto.ts`
- `apps/backend/src/modules/system/dto/update-welcome-document.dto.ts`

新增接口：

- `POST /api/v1/admin/documents/format-text-import`
- `GET /api/v1/system/welcome-document`
- `GET /api/v1/system/admin/welcome-document`
- `PUT /api/v1/system/admin/welcome-document`

## 4. 前端行为变化

### 4.1 游客页

- 不再显示欢迎稿于左侧文档树
- 未选择真实文档时：
  - 右侧显示系统欢迎文档
- 选择真实文档后：
  - 正常切换到选中文档内容

### 4.2 管理员页

- 未选择真实文档时：
  - 默认编辑系统欢迎文档
- 参数设置弹窗：
  - 不会因文本选择误关闭
- 一键转换按钮：
  - 改为走 Python + 大模型
- 一键转换时：
  - 底部显示阶段性进度条
  - 阶段包含：
    - `大模型处理中`
    - `导入处理中`
    - `导入完成`

## 5. 关键修改文件

### 后端

- `apps/backend/src/modules/document/document.service.ts`
- `apps/backend/src/modules/document/document.controller.ts`
- `apps/backend/src/modules/document/document-preview.util.ts`
- `apps/backend/src/modules/document/dto/format-text-import.dto.ts`
- `apps/backend/src/modules/search/search.service.ts`
- `apps/backend/src/modules/system/system.controller.ts`
- `apps/backend/src/modules/system/system.service.ts`
- `apps/backend/src/modules/system/dto/update-welcome-document.dto.ts`

### 前端

- `apps/frontend/src/lib/api.ts`
- `apps/frontend/src/stores/workspace.ts`
- `apps/frontend/src/views/AdminPage.vue`
- `apps/frontend/src/views/GuestPage.vue`
- `apps/frontend/src/components/GovDocPreview.vue`
- `apps/frontend/src/components/WorkspaceTree.vue`

### 脚本

- `scripts/text2md_formatter.py`
- `scripts/word2md_converter.py`

## 6. 已执行验证

本轮多次执行并确认通过：

```bash
npm run build
```

结果：

- 后端构建通过
- 前端构建通过

仍存在但当前不阻断的现象：

- Vite 构建时仍会出现 `static/fonts/SourceHanSansSC-*.otf` 未在构建时解析的 warning
- 该 warning 当前不影响产物生成和功能联调

## 7. 当前行为边界

### 欢迎文档

- 是系统级本地文件，不是数据库中的普通文档
- 游客不能在树中看到它
- 管理员可以编辑并保存它
- 它用于游客未选中文档时的兜底展示

### 文档标题

- 标题仍按“全局唯一”方向处理
- 但创建/导入/改名时不再因重名直接失败，而是自动追加序号避让
- 如果后续要支持“同名不同目录”，仍需要重新调整这条规则和相关查询逻辑

### 一键转换MD格式

- 依赖后端能成功调用 Python
- 依赖模型 API Key 配置正确
- 当前默认策略是：
  - 真实文档就地写回
  - 非真实文档则新建导入

## 8. 下次续接建议

如果继续开发，建议优先按以下顺序处理：

1. 实机验证一轮“一键转换md格式”完整链路
2. 在页面上把后端错误信息进一步友好化
3. 评估是否需要把标题唯一约束改成“同文件夹唯一”或“全局唯一”
4. 补充针对欢迎文档和 AI 格式化流程的 smoke / 最小接口验证
5. 检查 Python 依赖安装说明是否需要补入 `README.md`

## 9. 明确结论

截至 `2026-03-28 20:45`，本项目已经完成以下一组关键闭环：

- 文档本地持久化
- 欢迎页系统化持久化
- 管理员欢迎稿可编辑
- 游客欢迎页不可选不可见
- 拖拽移动修复
- 参数设置弹窗误关闭修复
- AI 文本格式化接入
- 标题唯一约束
- AI 转换两阶段进度展示

后续继续开发时，应以当前这套行为作为基线，不要回退到浏览器本地欢迎稿或前端本地 Markdown 规整逻辑。

## 10. 2026-03-30 补充记录

本节补记 `2026-03-30` 这一天已确认完成的开发与维护事项，用于修正第 2 节里的历史描述，并记录今天新增的可运行前提。

### 10.1 `.docx` 导入依赖问题已定位并补齐

管理员页在导入 `.docx` 文档时，曾出现：

- `No available converter for .docx`
- Python 子进程 traceback 指向 `scripts/word2md_converter.py`
- 根因是运行环境缺少 `python-docx`

今天已完成：

- 新增 `scripts/requirements.txt`
- 执行 `py -3 -m pip install -r scripts/requirements.txt`
- 确认 `openai` 与 `python-docx` 均可正常导入

当前结论：

- `.docx` 导入链路依赖 Python 包：
  - `openai`
  - `python-docx`
- 仍依赖 `DEEPSEEK_API_KEY`

### 10.2 `scripts/` 目录已清理并补文档

今天对根目录 `scripts/` 做了结构整理：

- 保留运行时必需文件：
  - `sample.md`
  - `text2md_formatter.py`
  - `word2md_converter.py`
- 新增说明文件：
  - `scripts/README.md`
  - `scripts/requirements.txt`
- 已删除测试/样例产物：
  - `scripts/test2.docx`
  - `scripts/test2_converted.txt`
  - `scripts/任意名称验证_道路运输办法.docx`

当前建议：

- 以后新增示例文件时，不要直接堆放在 `scripts/` 根目录
- 优先放入 `docs/` 或单独的示例目录

### 10.3 文档标题冲突已改为自动容错

此前第 `2.8` 节记录的是“标题冲突直接报 400”。  
今天这个行为已经调整，避免在管理员导入或创建同名文档时流程过于僵硬。

现在后端最新行为为：

- 创建文档时若标题已存在：
  - 自动改为 `原标题 (2)`、`原标题 (3)` ...
- AI 文本整理导入时若标题冲突：
  - 自动生成可用标题继续导入
- 修改已有文档标题时若与其他文档重名：
  - 自动生成可用标题并保存

例如：

- 已存在 `领导课题_优化版`
- 再次导入时，会自动落成：
  - `领导课题_优化版 (2)`

这意味着：

- 标题仍按“全局唯一”目标管理
- 但不再把标题冲突直接暴露为前端 400 错误

### 10.4 文档状态/失效日期字段在今天有继续修补

今天已确认以下文件发生变化：

- `apps/backend/src/modules/document/entities/document.entity.ts`
- `db/migrations/0010_document_status_expiry.sql`

该迁移的目的包括：

- 给 `documents` 表补齐 `status`
- 给 `documents` 表补齐 `expiry_date`
- 修补 `void_reason`
- 修补 `void_at`
- 添加状态和失效日期索引

说明：

- 这部分是对前面若干迁移缺口的补偿性修复
- 后续若继续做提醒、统计、报表功能，这些字段会成为基础依赖

### 10.5 对当前工作树的 review 结论

今天复核 `dev_20260328_2045.md` 与当前工作树后，有两个应特别注意的结论：

1. 第 `2.8` 节原先关于“标题冲突直接报错”的描述已经过期，现已在本文修正。
2. 当前工作树里已经存在 `reminder`、`reports`、`statistics` 模块和多份新增迁移，但本文件此前并未覆盖它们的行为边界；如果后续继续沿这些模块开发，应单独补一份更完整的续接记录，避免继续把旧文档当成全量现状。

### 10.6 今天的验证结果

今天再次执行并确认通过：

```bash
npm run build
```

结果：

- 后端构建通过
- 前端构建通过

仍保留的说明：

- Vite 构建阶段的字体资源 warning 依旧存在
- 当前不影响构建产物生成
